apiVersion: v1
kind: Service
metadata:
  name: bookclub-django
  namespace: bookclub
spec:
  type: NodePort

  selector:
    app: bookclub-django

  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30081